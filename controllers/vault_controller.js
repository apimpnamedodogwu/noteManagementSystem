const Vault = require('../models/vaults_models');
const User = require('../models/users_models');
const Note = require('../models/notes_models');

exports.addANoteToVault = async (req, res) => {
    
}